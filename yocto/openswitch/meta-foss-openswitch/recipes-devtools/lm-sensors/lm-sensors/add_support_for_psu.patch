diff --git a/lib/sensors.h b/lib/sensors.h
index 3e595d5..2e35595 100644
--- a/lib/sensors.h
+++ b/lib/sensors.h
@@ -152,6 +152,7 @@ typedef enum sensors_feature_type {
 	SENSORS_FEATURE_HUMIDITY	= 0x06,
 	SENSORS_FEATURE_PWM	        = 0x07,
 	SENSORS_FEATURE_LED	        = 0x08,
+	SENSORS_FEATURE_PSU			= 0x09,
 	SENSORS_FEATURE_MAX_MAIN,
 	SENSORS_FEATURE_VID		= 0x10,
 	SENSORS_FEATURE_INTRUSION	= 0x11,
@@ -252,6 +253,10 @@ typedef enum sensors_subfeature_type {
     SENSORS_SUBFEATURE_LED_NAME,
     SENSORS_SUBFEATURE_LED_CAPABILITY,
 
+	SENSORS_SUBFEATURE_PSU_STATUS = SENSORS_FEATURE_PSU << 8,
+	SENSORS_SUBFEATURE_PSU_PG_STATUS,
+	SENSORS_SUBFEATURE_PSU_ALARM_STATUS,
+
 	SENSORS_SUBFEATURE_VID = SENSORS_FEATURE_VID << 8,
 
 	SENSORS_SUBFEATURE_INTRUSION_ALARM = SENSORS_FEATURE_INTRUSION << 8,
diff --git a/lib/sysfs.c b/lib/sysfs.c
index 2e79121..55d1982 100644
--- a/lib/sysfs.c
+++ b/lib/sysfs.c
@@ -197,6 +197,7 @@ char *get_feature_name(sensors_feature_type ftype, char *sfname)
 	case SENSORS_FEATURE_CURR:
 	case SENSORS_FEATURE_HUMIDITY:
 	case SENSORS_FEATURE_INTRUSION:
+	case SENSORS_FEATURE_PSU:
 		underscore = strchr(sfname, '_');
 		name = strndup(sfname, underscore - sfname);
 		if (!name)
@@ -352,6 +353,13 @@ static const struct subfeature_type_match led_matches[] = {
 	{ NULL, 0 }
 };
 
+static const struct subfeature_type_match psu_matches[] = {
+	{ "status", SENSORS_SUBFEATURE_PSU_STATUS },
+	{ "pg_status", SENSORS_SUBFEATURE_PSU_PG_STATUS },
+	{ "alarm_status", SENSORS_SUBFEATURE_PSU_ALARM_STATUS },
+	{ NULL, 0 }
+};
+
 static struct feature_type_match matches[] = {
 	{ "temp%d%c", temp_matches },
 	{ "in%d%c", in_matches },
@@ -364,6 +372,7 @@ static struct feature_type_match matches[] = {
 	{ "humidity%d%c", humidity_matches },
 	{ "pwm%d%c", pwm_matches },
 	{ "led%d%c", led_matches },
+	{ "psu%d%c", psu_matches },
 };
 
 /* Return the subfeature type and channel number based on the subfeature
@@ -503,6 +512,7 @@ static int sensors_read_dynamic_chip(sensors_chip_features *chip,
 		case SENSORS_FEATURE_HUMIDITY:
 		case SENSORS_FEATURE_PWM:
 		case SENSORS_FEATURE_LED:
+		case SENSORS_FEATURE_PSU:
 			nr--;
 			break;
 		default:
